# guardrails secret/PII scan allowlist.
# Any ADDED diff line matching one of these extended-regex patterns is skipped.
# One regex per line; # starts a comment. Add legitimate test/example values here.

# Placeholders
change-me
placeholder
REDACTED
dummy
example[_-]?value
your-.*-here

# Env indirection (not a literal secret)
process\.env
import\.meta\.env
os\.environ
os\.getenv
\$\{[A-Za-z_][A-Za-z0-9_]*\}
<[a-zA-Z._-]+>

# Test / example emails & domains
@example\.(com|org|net)
@test\.
@local\.test

# Own public contact addresses (fine to appear in code/docs)
kulykivska\.yuliia@gmail\.com
hello@racemodel\.io

# Known local/test fixtures
localhost
127\.0\.0\.1
