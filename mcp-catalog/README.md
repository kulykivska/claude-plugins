# mcp-catalog

This plugin ships no live MCP config: `.mcp.json.example` is a catalog.
To activate a server in a project, copy the entry you need into that
project's `.mcp.json` and fill the credentials. Nothing auto-connects.
